# แผนงาน Mochi Tap

เอกสารนี้สรุปการตัดสินใจเชิงออกแบบทั้งหมด ว่าทำไมถึงเลือกแบบนี้ และจะต่อยอดไปทางไหนได้

## เรื่องตัวละคร Sanrio — อ่านก่อน

โปรเจกต์นี้**ไม่ได้ใช้ Cinnamoroll** และตัวละครทุกส่วนออกแบบขึ้นใหม่ทั้งหมด

Cinnamoroll เป็นเครื่องหมายการค้าจดทะเบียนของ Sanrio ซึ่งบังคับใช้สิทธิ์อย่างจริงจัง
ตัวละครที่มีลิขสิทธิ์ต่างจากสไตล์งานทั่วไปตรงที่ **รูปร่างเฉพาะตัวของมันคืองานที่ได้รับความคุ้มครอง**
การวาดขึ้นใหม่เองก็ยังนับเป็นงานดัดแปลง ไม่ได้ทำให้พ้นจากการคุ้มครอง

พอเอาขึ้น GitHub Pages แบบสาธารณะแล้ว ความเสี่ยงที่เกิดขึ้นจริงคือ

- โดน DMCA takedown ซึ่ง GitHub จะปิด repo ทั้งอัน ไม่ใช่แค่ลบไฟล์รูป
- ถ้าโดนซ้ำ บัญชี GitHub อาจถูกระงับ
- ถ้าวันหนึ่งอยากทำเป็นงานจริงหรือมีรายได้ ประวัตินี้จะกลายเป็นปัญหา

ทำเล่นในเครื่องตัวเองไม่มีใครว่า แต่พอเผยแพร่แล้วเป็นคนละเรื่องกัน
เลยเลือกออกแบบตัวละครใหม่ที่ให้ความรู้สึกใกล้เคียง — ตัวกลมขาวนวล หูยาวห้อย แก้มชมพู ตากลมดำ
ซึ่งเป็นลักษณะทั่วไปที่ไม่ผูกกับตัวละครใดตัวหนึ่ง

ถ้าอยากปรับให้ถูกใจขึ้น แก้ได้ที่ออบเจ็กต์ `LOOK` ใน `Character.tsx` ซึ่งคุมสัดส่วนทุกอย่าง
วิธีทำอยู่ใน [`docs/CHARACTER.md`](docs/CHARACTER.md) — ยืดหู เปลี่ยนสี ขยายหัว ทำได้ในไม่กี่บรรทัด

อยากใช้ตัวละคร Sanrio จริง ๆ มีทางที่ถูกต้องอยู่: Sanrio มีระบบขอไลเซนส์สำหรับนักพัฒนา
ติดต่อผ่านฝ่าย licensing ของ Sanrio ในภูมิภาคได้ แต่ปกติจะรับเฉพาะงานเชิงพาณิชย์ที่มีสเกลระดับหนึ่ง

## คำตอบสามข้อที่ถามไว้ตอนแรก

**ทำ single page บน GitHub Pages ดีไหม** — ดี เหมาะมาก เพราะแอปนี้ไม่มี routing ไม่มี SEO ที่ต้องแคร์ และไม่มีข้อมูลผู้ใช้ที่ต้องปกป้อง GitHub Pages ให้พื้นที่ฟรี แบนด์วิดท์ 100GB ต่อเดือน และมี HTTPS ให้ในตัว

**ต้องมี DB ไหม** — ไม่ต้อง ถ้านับแค่ยอดของผู้ใช้เอง `localStorage` เพียงพอทุกอย่าง จะต้องมีก็ต่อเมื่ออยากได้ยอดรวมของทุกคน ซึ่งทำไว้ให้แล้วแบบเปิด/ปิดได้

**ต้องใช้อะไรบ้าง** — Node.js กับบัญชี GitHub เท่านั้น ส่วน Supabase เป็นของเสริม

## Stack และเหตุผล

| ส่วน | เลือก | ทำไม |
|---|---|---|
| Build | Vite + React + TypeScript | build ออกมาเป็น static ล้วน วางบน Pages ได้ตรง ๆ |
| CSS | Tailwind 3 | คุมเลย์เอาต์มือถือได้เร็ว ส่วนแอนิเมชันเขียน CSS เองใน `index.css` |
| ตัวละคร | SVG เขียนมือ + CSS keyframes | ไม่มีไฟล์ภาพเลย โหลดทันที เปลี่ยนสีหน้าได้ 18 แบบจากตัวแปรเดียว |
| ฉาก | SVG เขียนมือ | ไฟล์เล็กกว่ารูป และปรับสีตามธีมได้ |
| แอนิเมชัน | CSS ล้วน | ไม่ต้องพึ่ง Framer Motion ลด bundle ไปราว 50KB |
| เสียง | Web Audio สังเคราะห์สด | ไม่ต้องมีไฟล์ mp3 ไม่มีดีเลย์ตอนแตะ เพิ่มเสียงใหม่ใช้เวลาไม่กี่นาที |
| เก็บข้อมูล | localStorage | ไม่ต้องมี backend |
| ยอดรวมทุกคน | Supabase (ไม่บังคับ) | free tier เหลือเฟือ ดูตารางด้านล่าง |
| Deploy | GitHub Actions → Pages | push แล้วขึ้นเอง |

**dependency ตอนรันมีแค่สามตัว**: react, react-dom, @supabase/supabase-js

## ระบบสุ่มท่าทาง

ทุกครั้งที่แตะ จะไล่ตามลำดับนี้ หยุดที่เงื่อนไขแรกที่ตรง

1. **อยู่ในโหมดปลอบใจ** — การแตะกลายเป็นการข้ามไปบรรทัดถัดไปของบทสนทนา
2. **แตะครบหลักไมล์** — ยอดสะสมตรงตัวเลขใน `events.json` พอดี แต่ละหมุดยิงครั้งเดียว
3. **แตะรัว** — เกิน 9 ครั้งใน 3 วินาที ออกท่ามึนหัว
4. **สุ่มถ่วงน้ำหนัก** — คัดเฉพาะท่าที่เข้ากับโซนที่แตะ แล้วสุ่มตาม `weight` พร้อมเลี่ยง 3 ท่าล่าสุด

ส่วนตอนเปิดหน้าเว็บจะเช็ค: เข้าครั้งแรก → หายไปเกิน 3 วัน → ช่วงเวลาของวัน
และถ้าอยู่เฉย ๆ เกิน 35 วินาที น้องจะหลับ

## ทำให้น้องดูมีชีวิตเหมือนการ์ตูน

นี่คือส่วนที่ลงแรงมากที่สุด เพราะตัวการ์ตูนที่ยืนนิ่งจะดูเป็นรูปปั้นทันที ใช้สามชั้นซ้อนกัน

**ชั้นที่ 1 — หายใจตลอดเวลา** ท่า idle ขยับขึ้นลง 9px พร้อมบีบตัวเล็กน้อย วนไม่มีหยุด

**ชั้นที่ 2 — กะพริบตาอัตโนมัติ** ทำด้วย CSS ล้วน ไม่ใช้ JS เลย เปลือกตาสีเดียวกับตัวซ่อนอยู่ 93% ของรอบ
แล้วโผล่มาแค่ช่วงสั้น ๆ รอบละ 5.4 วินาที สุ่ม delay เริ่มต้นทุกครั้งที่เปลี่ยนท่า จะได้ไม่เป็นจังหวะเดิม

**ชั้นที่ 3 — ท่าเล็ก ๆ ที่ทำเอง** ทุก 6–13 วินาทีตอนไม่มีใครแตะ น้องจะมองซ้ายขวา กระดิกหู
ยืดเส้น ฮัมเพลง โยกตัว หรือเหม่อ ท่าพวกนี้ไม่มีข้อความและไม่นับยอดแตะ
ที่สำคัญคือมันขยับ**เฉพาะบางส่วนของตัว** เช่นกระดิกหูก็ขยับแค่หู ตัวยังหายใจอยู่เหมือนเดิม
ซึ่งเป็นสิ่งที่ทำให้ดูเป็นการ์ตูนมากกว่าการขยับทั้งตัวพร้อมกัน

เทคนิคเสริมที่ใช้: หูขวากระดิกช้ากว่าหูซ้าย 0.15 วินาที และทุกท่ากระโดดจะย่อตัวลงก่อนหนึ่งเฟรม

## โหมดปลอบใจ

กดปุ่ม "วันนี้เป็นอะไรมา" → เลือกอารมณ์จาก 7 หัวข้อ → น้องพูดบทเปิด 2–3 บรรทัด
→ ขึ้นตัวเลือกย่อย 2–3 อัน → น้องตอบ 3–4 บรรทัด → ปิดเอง

แต่ละบรรทัดค้างตามความยาวข้อความ (อย่างน้อย 2.4 วินาที + 75ms ต่อตัวอักษร)
แตะที่ตัวน้องเพื่อข้ามไปบรรทัดถัดไปได้ เหมือนอ่าน visual novel

หัวข้อที่มี: เหงา / ไม่สบายใจ / เสียใจ / เศร้า / เหนื่อย / อยากได้กำลังใจ / วันนี้โอเคดี

### สองเรื่องที่ตั้งใจออกแบบไว้

**มีทางออกไปหาความช่วยเหลือจริง** หัวข้อ "ไม่สบายใจ" กับ "เศร้า" มีตัวเลือกอย่าง
"มันหนักเกินจะแบกคนเดียว" และ "รู้สึกแบบนี้มานานแล้ว" ซึ่งน้องจะบอกตรง ๆ ว่าตัวเองเป็นแค่ตัวการ์ตูน
แล้วชี้ไปที่คนที่ไว้ใจได้หรือสายด่วนสุขภาพจิต 1323 ไม่ใช่การพยายามแก้ปัญหาให้เอง

เพราะแอปแบบนี้มีโอกาสสูงที่คนจะเปิดตอนตีสองในคืนที่แย่จริง ๆ ถ้าตอบแค่ "สู้ ๆ นะ" อย่างเดียวมันไม่พอ
แนะนำให้เก็บตัวเลือกพวกนี้ไว้ ถ้าจะแก้ก็เปลี่ยนแค่ถ้อยคำ

**เก็บข้อมูลให้น้อยที่สุด** ตัวเลือกย่อยที่กด (เช่น "อยากร้องไห้" หรือ "รู้สึกแบบนี้มานานแล้ว")
ไม่ถูกบันทึกที่ไหนเลย ทั้งใน localStorage และบนเซิร์ฟเวอร์ คนที่กดว่า "เศร้า" ไม่ควรต้องกังวลว่ามันจะไปโผล่ที่ไหน

สิ่งเดียวที่จำไว้คือ **หัวข้อไหนเคยเปิดดูแล้วบ้าง** (แค่ชื่อหัวข้อ ไม่ใช่คำตอบ) เก็บใน localStorage
เครื่องนั้นเท่านั้น เพื่อรู้ว่าเปิดครบ 7 หัวข้อหรือยัง — ใช้ปลดล็อกป้ายพิเศษของ Dev ที่มุมขวาล่าง
(ดูหัวข้อถัดไป) ไม่ส่งออกจากเครื่องและลบได้ด้วยปุ่มรีเซ็ตในแผงตั้งค่า

## ป้ายพิเศษของ Dev

หลังเปิดดูครบทุกหัวข้อในโหมดปลอบใจ จะมีป้ายเล็ก ๆ โผล่ที่มุมขวาล่างของจอ กดแล้วสุ่มข้อความ
ให้กำลังใจจาก "Dev" ขึ้นมา — ตั้งใจให้เป็นเซอร์ไพรส์เล็ก ๆ สำหรับคนที่เปิดใจคุยกับน้องจนครบทุกเรื่อง
ไม่ใช่รางวัลที่ต้องพยายามไล่ล่า จึงไม่มี progress bar หรือคำใบ้บอกว่าเหลืออีกกี่หัวข้อ

ข้อความมีสองโทนปนกัน — แบบที่ชวนให้รู้สึกว่ามีคนพร้อมรับฟัง ("มีปัญหาอะไรก็ปรึกษา Dev ได้เลยนะ")
และแบบให้กำลังใจเฉย ๆ ("เธอไม่ได้อยู่คนเดียวหรอกนะ") สุ่มมาโชว์ทีละอันและเลี่ยงไม่ให้ซ้ำติดกัน
ปรับข้อความหรือเงื่อนไขปลดล็อกได้ที่ `docs/CONTENT.md` หัวข้อ 6

## Counter ที่โชว์มุมจอ

- **แตะรอบนี้** — ตัวใหญ่สุด รีเซ็ตทุกครั้งที่โหลดหน้าใหม่
- **สะสมทั้งหมด** — สะสมข้ามการเข้าเว็บ เก็บใน localStorage
- **ทุกคนรวมกัน** — จาก Supabase ซ่อนอัตโนมัติถ้าไม่ได้ตั้งค่า

ด้านล่างมีแถบบอกว่าอีกกี่ครั้งจะปลดล็อกฉากถัดไป

## ประหยัดโควต้า Supabase อย่างไร

ถ้ายิง API ทุกครั้งที่แตะ คนเดียวแตะรัว ๆ นาทีเดียวก็หลายร้อย request
โปรเจกต์นี้จึง**สะสมยอดไว้ในหน่วยความจำ แล้วส่งรวบทุก 5 วินาที** และส่งทันทีตอนปิดแท็บหรือสลับแอป
ตัวเลขบนจอบวกให้เห็นทันทีแบบ optimistic แล้วค่อยแก้เป็นค่าจริงจากเซิร์ฟเวอร์

ผลคือ ผู้ใช้หนึ่งคนเล่น 10 นาที = ราว 120 request แทนที่จะเป็นหลายพัน

| ทรัพยากร | Supabase Free | แอปนี้ใช้ |
|---|---|---|
| Database | 500MB | ตารางเดียว หนึ่งแถว ไม่ถึง 1KB |
| API requests | ไม่จำกัดจำนวน | รวบส่งทุก 5 วินาที |
| Bandwidth | 5GB/เดือน | ตอบกลับครั้งละไม่กี่ไบต์ |
| หยุดพักโปรเจกต์ | ถ้าไม่มีใครใช้ 7 วัน | กดปลุกใน dashboard ได้ |

## รองรับมือถือ

- `viewport-fit=cover` + `env(safe-area-inset-bottom)` สำหรับ iPhone ที่มีรอยบาก
- `100dvh` แทน `100vh` ไม่ให้แถบ address bar บังเนื้อหา
- Pointer Events แทน click ตอบสนองเร็วกว่าราว 300ms
- `touch-action: manipulation` กัน double-tap zoom
- `overscroll-behavior: none` กันหน้าเด้งตอนลาก
- ปลดล็อก AudioContext ตอนแตะครั้งแรก เพราะ iOS บล็อกเสียงที่ไม่ได้มาจาก user gesture
- สั่นด้วย `navigator.vibrate` ตอนแตะ (ทำงานบน Android)
- แอนิเมชันใช้แค่ `transform` กับ `opacity` เพื่อให้ GPU จัดการ
- พารัลแลกซ์ตามการเอียงเครื่อง ตกลงมาใช้เมาส์บนเดสก์ท็อป
- เคารพ `prefers-reduced-motion` ปิดการเคลื่อนไหวทั้งหมดถ้าผู้ใช้ตั้งไว้

## แผนต่อยอด

**ทำแล้ว** — ท่าสุ่ม 12 ท่า + ท่าแอมเบียนต์ 8 ท่า + ท่าปลอบใจ 8 ท่า, สีหน้า 18 แบบ,
โซนแตะ 5 โซน, กะพริบตาอัตโนมัติ, หลักไมล์ 7 จุด, ฉาก 7 ฉากแบบมีรายละเอียด,
โหมดปลอบใจ 7 หัวข้อ, ป้ายพิเศษของ Dev หลังเปิดครบทุกหัวข้อ, เสียง 14 แบบ, อัปโหลดรูปเอง,
พารัลแลกซ์, counter สามชั้น, Supabase แบบไม่บังคับ, CI deploy, สคริปต์ตรวจข้อมูล

**รอบถัดไปถ้าอยากไปต่อ**

- ทำเป็น PWA ด้วย `vite-plugin-pwa` เพิ่มลงหน้าจอโฮมและเล่นออฟไลน์ได้
- ปุ่มแชร์ภาพหน้าจอ ใช้ `html2canvas` + Web Share API
- เปลี่ยนตัวละครจาก SVG เป็น Rive เมื่ออยากได้ท่าที่ลื่นกว่านี้ (แก้แค่ `Character.tsx` ไฟล์เดียว)
- ระบบ "ค่าความสนิท" ที่ค่อย ๆ ลดถ้าหายไปนาน แล้วบทพูดเปลี่ยนตามระดับ
- บันทึกอารมณ์ย้อนหลัง (ต้องถามผู้ใช้ก่อนว่าอยากให้เก็บมั้ย และเก็บในเครื่องเท่านั้น)
- ข้อความตามวันสำคัญ เช่น วันเกิดผู้ใช้ ปีใหม่
- เพิ่มตัวละครที่สอง สลับได้ในแผงตั้งค่า
